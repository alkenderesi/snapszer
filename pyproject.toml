[project]
name = "snapszer"
version = "0.1.0"
description = "Snapszer card game"
readme = "README.md"
authors = [
    { name = "alkenderesi", email = "43848302+alkenderesi@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
snapszer = "snapszer:main"

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"
